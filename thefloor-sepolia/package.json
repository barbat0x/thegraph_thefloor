{
  "name": "thefloor-sepolia",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ barbat0x/thefloor-sepolia",
    "create-local": "graph create --node http://localhost:8020/ barbat0x/thefloor-sepolia",
    "remove-local": "graph remove --node http://localhost:8020/ barbat0x/thefloor-sepolia",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 barbat0x/thefloor-sepolia",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.61.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
